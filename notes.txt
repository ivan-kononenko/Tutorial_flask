flaskr
	локація собсна аплікухи

hello.py
	???

auth.py
	блупрінт для аутентифікації користувача. Блупрінт = спосіб орагнізації пов'язаних в'юшок і всякого іншого кода. Щоб не повторювати себе при написанні кода використовуємо блупрінти, алсо щоб не реєструвати кожну в'юшку окремо.

db.py
	дічь, яка відповідає за конект операції до бази даних. З важливого: g - спецоб'єкт який зберігає дані, котрі можуть використовуватися різними функціями в межах одного реквесту, є унікальною для кожного реквесту.

__init__.py
	так званий "application factory". створює інстанцію flask.Flask, котрий є центральним реєстром для всіх в'юшок, УРЛов і так далі. також __ініт__ виступає в ролі окремого пекеджа для пайтона. Нашо воно? для створення та реєстрації всіх аплікух(принаймні на даний момент)

__pycache__

schema.sql
	собсна БД
